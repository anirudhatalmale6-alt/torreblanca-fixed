TORRE BLANCA - CHANGES LOG
===========================
Date: 2026-01-15 (v2 - Complete Fix)

ISSUES FIXED:
-------------

1. COMPLETE APP.JS REPLACEMENT (Desktop & Mobile)
   - PROBLEM: piso-3, piso-4, piso-5 had completely different JavaScript code
     that was not matching piso-2 (the technical reference). This caused:
     * Broken hover behavior
     * Giant photos in "Todas las fotos" gallery
     * Various panel bugs
   - FIX: Replaced app.js in piso-3, piso-4, piso-5 with exact copy of piso-2/app.js
     Only changed the APT constant:
     * piso-3: apto-02
     * piso-4: apto-03
     * piso-5: apto-04
   - This fixes:
     * Hover behavior (masks now work correctly)
     * Gallery "Todas las fotos" (photos now display with correct cardRow layout)
     * Panel behavior (proper open/close)

2. STYLES.CSS NORMALIZED (Desktop & Mobile)
   - PROBLEM: piso-3, piso-4, piso-5 had different CSS causing visual issues
   - FIX: Copied piso-2/styles.css to all other floors
   - This ensures consistent gallery card sizing (.cardRow with 220px images)

3. MOBILE VERSION - Assets (from v1)
   - Assets were already copied to mobile piso-4 and piso-5

FILES MODIFIED:
---------------
Desktop (torre-blanca):
- piso-3/app.js (complete replacement with piso-2 template)
- piso-3/styles.css (replaced with piso-2 version)
- piso-4/app.js (complete replacement with piso-2 template)
- piso-4/styles.css (replaced with piso-2 version)
- piso-5/app.js (complete replacement with piso-2 template)
- piso-5/styles.css (replaced with piso-2 version)

Mobile (torre-blanca-m):
- piso-3/app.js (complete replacement with piso-2 template)
- piso-3/styles.css (replaced with piso-2 version)
- piso-4/app.js (complete replacement with piso-2 template)
- piso-4/styles.css (replaced with piso-2 version)
- piso-5/app.js (complete replacement with piso-2 template)
- piso-5/styles.css (replaced with piso-2 version)

RULES FOLLOWED:
---------------
- NO files renamed
- NO folder structure changed
- NO assets moved between apartments
- All paths built dynamically from APT/APTO_PREFIX constants
- piso-2 used as technical template (as specified in documentation)

TESTING:
--------
- All floors now use identical code (only APT constant differs)
- Gallery "Todas las fotos" shows cardRow layout (220px x 130px images)
- Hover masks work correctly on all floors
- Panels open/close properly
